# This is an example file. Copy it to .env.local for development.
# The VITE_ prefix is required by Vite to expose these variables to the client-side code.

# The API key for accessing the backend management endpoints.
# This MUST match the MASTER_API_KEY on the backend.
VITE_API_KEY="super-secret-dev-key"

# The base URL for the API. During development with Vite's proxy, this is not strictly
# needed, but it's good practice for production builds.
# In our docker-compose setup, this will be handled by Nginx, so the default '/api/v1' is fine.
VITE_API_BASE_URL="/api/v1"